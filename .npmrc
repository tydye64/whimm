# expo-modules-core's peer range for react-native-worklets tops out below
# what react-native-reanimated 4.x actually needs (0.12.x) — a real,
# harmless mismatch, not a broken install. Without this, a plain
# `npm install` on a fresh clone fails with ERESOLVE.
legacy-peer-deps=true
